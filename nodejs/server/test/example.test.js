const { expect } = require('chai');

describe('This is a category for tests in the same topic', () => {
    it('should be true', () => {
        expect(true).to.be.true;
        expect(true).to.equal(true);
    });
});
